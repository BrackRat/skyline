import 'package:flutter/material.dart';

class BottomReminder extends StatelessWidget {
  const BottomReminder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("--------"),
    );
  }
}
